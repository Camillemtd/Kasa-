import React from 'react';
import Header from '../component/Header';

const Home = () => {
    return (
        <div>
            <Header/>
            home 
        </div>
    );
};

export default Home;